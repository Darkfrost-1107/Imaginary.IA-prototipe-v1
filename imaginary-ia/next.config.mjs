/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'res.cloudinary.com',
        'cdn.leonardo.ai'
      ], // Añade Cloudinary aquí
    },
  };

  
export default nextConfig;
